awe_modTestAudioInstance* S = (awe_modTestAudioInstance*)pInstance;
if (S->coeff[0] == 0)
    S->coeff[0] = 0.1;
else
    S->coeff[0] = S->coeff[0];

if (S->gain != S->gain)
    S->gain == S->gain;
return 0;