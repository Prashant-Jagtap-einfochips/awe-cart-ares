function GetNextXmlElementNode
%  GetNextNode : Given a node in an active xml document, returns the next
%  node in a depth-first search of the DOM node tree.
%  Usage:
%     node = GetNextNode(node)
