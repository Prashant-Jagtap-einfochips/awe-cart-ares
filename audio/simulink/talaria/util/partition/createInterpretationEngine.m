function createInterpretationEngine
%  createInterpretationEngine creates the Talaria translaiton engine for the
%  specified model
%  tagModelPath is the full path to the tagged model (used to make a copies).
%  strCompatDefine is a prepared #define with the TOP compat numbers to
%  include in the translation engine.
