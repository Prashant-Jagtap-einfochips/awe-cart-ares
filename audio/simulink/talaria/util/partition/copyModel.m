function copyModel
% copyModel Makes a copy of a model
%    copyModel(M, N, D) copies model M to a new file N, and puts it in
%    directory D. If the first character of D is "=" the directory is
%    created parallel of the current code generation folder. Otherwise, the
%    directory is a sub-directory based on the model's location.
