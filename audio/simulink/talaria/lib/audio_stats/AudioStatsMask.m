classdef AudioStatsMask
properties
  %  task ID must be the last option. It is only available as a code gen
  %  macro:
macroOnlyIdx
end
methods
function Transform158
   %  Transform function automatically created by AutomateMask
end
function CreateMaskDialog
   %  This function helps to create the library block - Gets it close.
end
function CB_IsMacroNameEval
   %  Called when the checkbox for using eval-vs-not-eval name changes
end
function CB_MacroNameRootEval
   %  Live update of the dialog has been delayed until block
   %  initialization time to better support evaluted expressions
end
function CB_MacroNameRoot
   %  Live update of the dialog has been delayed until block
   %  initialization time to better support evaluted expressions
end
function AudioStatsMask
   %AudioStatsMask/AudioStatsMask is a constructor.
   %    obj = AudioStatsMask
   %
   %    Documentation for AudioStatsMask/AudioStatsMask
   %       doc AudioStatsMask
   %
end
end
end
